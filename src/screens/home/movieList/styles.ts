import { StyleSheet } from 'react-native';

export const movieListStyles = () =>
  StyleSheet.create({
    listContainer: {
      gap: 10,
    },
    noDataStyles: {
      color: 'gray',
      fontSize: 16,
    },
  });
