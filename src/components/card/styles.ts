import { StyleSheet } from 'react-native';

export const cardStyles = () =>
  StyleSheet.create({
    container: {
      width: 150,
    },
    imgContainer: {
      width: 150,
      height: 180,
      borderRadius: 8,
    },
  });
